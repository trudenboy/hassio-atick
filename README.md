# Home Assistant aTick BLE Integration

[![Tests](https://github.com/trudenboy/hassio-atick/actions/workflows/tests.yml/badge.svg)](https://github.com/trudenboy/hassio-atick/actions/workflows/tests.yml)
[![Validate](https://github.com/trudenboy/hassio-atick/actions/workflows/validate.yml/badge.svg)](https://github.com/trudenboy/hassio-atick/actions/workflows/validate.yml)
[![HACS Custom](https://img.shields.io/badge/HACS-Custom-orange.svg)](https://github.com/custom-components/hacs)

Интеграция для работы с устройством [aTick] - счетчиком воды с Bluetooth Low Energy (BLE).

## Возможности

✅ **Реализовано:**
- Автоматическое обнаружение устройств aTick через Bluetooth
- Поддержка переименованных устройств (поиск по UUID сервиса)
- Чтение показаний счетчиков воды (А и B) из BLE advertisements
- Поддержка зашифрованных данных с PIN-кодом
- Применение множителей (ratios) к показаниям счетчиков
- Восстановление показаний после перезагрузки
- Настраиваемый интервал опроса устройства
- Корректировка показаний через офсеты (начальные значения)
- Сервисы для ручного управления показаниями
- Сенсор уровня BLE сигнала (RSSI)

Можно одновременно использовать интеграцию Home Assistant и оригинальное мобильное приложение. ПИН-код для сопряжения устройства задается в приложении.

## Установка

### Требования
- Home Assistant версии 2024.2.0 или новее
- Bluetooth адаптер на сервере Home Assistant
- Устройство aTick с настроенным PIN-кодом

### Вариант 1. Через [HACS](https://www.hacs.xyz/) (рекомендуется)

1. HACS → Настройки → Пользовательские репозитории → Добавить ссылку на репозиторий
2. HACS → Интеграции → Добавить → aTick → Установить
3. Перезагрузить Home Assistant

### Вариант 2. Ручная установка

1. Скопировать директорию `custom_components/deembot_atick` в `/config/custom_components/`
2. Перезагрузить Home Assistant

## Настройка

### Первоначальная настройка

1. После перезагрузки устройство будет обнаружено автоматически, или:
   - Перейдите в **Настройки** → **Интеграции** → **Добавить интеграцию**
   - Найдите **aTick** в списке
2. Выберите устройство из списка обнаруженных
3. Введите PIN-код, настроенный в мобильном приложении aTick
4. Нажмите "Отправить"

> **Важно:** Для надежного обнаружения устройства рекомендуется закрыть мобильное приложение и отключить Bluetooth на телефоне во время первоначальной настройки.

### Настройка опций (дополнительно)

После установки можно настроить дополнительные параметры:

1. **Настройки** → **Интеграции** → **aTick** → **Настроить**
2. Доступные опции:
   - **Интервал опроса** (60-86400 секунд) - как часто запрашивать данные с устройства (по умолчанию: 24 часа)
   - **Офсет счетчика A** (м³) - начальное значение для корректировки показаний счетчика A
   - **Офсет счетчика B** (м³) - начальное значение для корректировки показаний счетчика B

## Использование

### Сенсоры

Интеграция создает следующие сенсоры:

- `sensor.atick_XXXX_water_counter_a` - Показания счетчика воды A (м³)
- `sensor.atick_XXXX_water_counter_b` - Показания счетчика воды B (м³)
- `sensor.atick_XXXX_bluetooth_signal` - Уровень BLE сигнала (dBm, отключен по умолчанию)

### Сервисы

#### `deembot_atick.set_counter_value`

Установить текущее значение счетчика воды вручную.

```yaml
service: deembot_atick.set_counter_value
data:
  entity_id: sensor.atick_xxxx_water_counter_a
  value: 123.45
```

#### `deembot_atick.reset_counter`

Сбросить счетчик на ноль или указанное значение.

```yaml
service: deembot_atick.reset_counter
data:
  entity_id: sensor.atick_xxxx_water_counter_b
  value: 0  # опционально, по умолчанию 0
```

### Примеры автоматизаций

#### Уведомление при высоком расходе воды

```yaml
automation:
  - alias: "Предупреждение о высоком расходе воды"
    trigger:
      - platform: state
        entity_id: sensor.atick_xxxx_water_counter_a
    condition:
      - condition: template
        value_template: >
          {{ (trigger.to_state.state | float - trigger.from_state.state | float) > 0.5 }}
    action:
      - service: notify.mobile_app
        data:
          title: "Высокий расход воды"
          message: "Обнаружен расход более 0.5 м³ за короткий период"
```

#### Ежедневная статистика потребления

```yaml
sensor:
  - platform: template
    sensors:
      daily_water_usage_a:
        friendly_name: "Расход воды за день (счетчик A)"
        unit_of_measurement: "m³"
        value_template: >
          {{ (states('sensor.atick_xxxx_water_counter_a') | float -
              state_attr('sensor.atick_xxxx_water_counter_a', 'last_reset_value') | float(0)) | round(3) }}
```

## FAQ

### Устройство не обнаруживается

1. Убедитесь, что Bluetooth включен на сервере Home Assistant
2. Проверьте, что устройство находится в радиусе действия (обычно до 10 метров)
3. Закройте мобильное приложение aTick и отключите Bluetooth на телефоне
4. Перезагрузите устройство aTick (отключите батарею на 10 секунд)
5. Попробуйте добавить интеграцию вручную

### Показания счетчика неправильные

1. Проверьте, что применяются правильные множители (ratios) - они загружаются с устройства
2. Используйте офсеты в настройках для корректировки начальных значений
3. Используйте сервис `set_counter_value` для ручной установки правильного значения

### Устройство переименовано и не находится

Начиная с версии 0.0.4, интеграция поддерживает поиск устройств по UUID сервиса, поэтому переименованные устройства должны обнаруживаться автоматически.

### Как изменить PIN-код

1. Измените PIN в мобильном приложении aTick
2. В Home Assistant: **Настройки** → **Интеграции** → **aTick** → **⋮** → **Настроить заново**
3. Введите новый PIN-код

## Roadmap

### ✅ Реализовано
- ✅ Корректный поиск устройства если оно переименовано
- ✅ Корректный расчёт с учётом множителя
- ✅ Корректировка показаний (задать начальные показания через офсеты)
- ✅ Настройки интервала опроса
- ✅ Сервисы для ручного управления показаниями
- ✅ Unit тесты
- ✅ CI/CD (GitHub Actions)

### 🔜 Планируется
- ⏳ Добавить иконку интеграции
- ⏳ Получение/Сохранение настроек устройства через GATT
- ⏳ Настройки энергосбережения (как в приложении)
- ⏳ Команды управления устройством
- ⏳ Интеграция с Energy Dashboard
- ⏳ Дополнительные диагностические сенсоры (батарея, статус шифрования)

## Contribution

Все желающие, кто может внести улучшения, присылайте Pull Requests!

### Для разработчиков

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Установите зависимости для разработки: `pip install -r requirements_test.txt`
4. Запустите тесты: `pytest tests/`
5. Запустите линтеры: `black custom_components/ && isort custom_components/ && flake8 custom_components/`
6. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
7. Запушьте в ветку (`git push origin feature/amazing-feature`)
8. Создайте Pull Request

## Troubleshooting

### Логи

Для включения подробного логирования добавьте в `configuration.yaml`:

```yaml
logger:
  default: info
  logs:
    custom_components.deembot_atick: debug
```

### Известные проблемы

- На некоторых системах может потребоваться дополнительная настройка Bluetooth адаптера
- При одновременной работе нескольких Bluetooth устройств возможны конфликты

## License

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.


#### Внешний вид
![1.png](./img/autodiscovery.png)
![1.png](./img/1.png)
